import 'package:flutter/material.dart';

//TODO Search page

class Search extends StatelessWidget {
  const Search({Key key}) : super(key: key);
// TODO APPBAR searchIcon \ searchbar \ exit or cancel button

// TODO blue bubbles with categories

//TODO my recent orders, uses the same element as Order again in profile page
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('search')),
    );
  }
}
